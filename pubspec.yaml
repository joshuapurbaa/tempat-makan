name: tempat_makan
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  theme:
    path: packages/theme
  app_state_manager:
    path: packages/app_state_manager
  go_router: ^5.1.6
  provider: ^6.0.4
  sign_in:
    path: packages/features/sign_in
  onboarding:
    path: packages/features/onboarding
  home:
    path: packages/features/home
  detail:
    path: packages/features/detail
  profile:
    path: packages/features/profile
  core:
    path: core
    
  responsive_framework: ^0.2.0
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/